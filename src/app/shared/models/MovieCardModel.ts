export interface MovieCardModel {
    id:        number;
    title:     string;
    posterUrl: string;
}
// not use class, use interface to define data model, beacuse interface is lightweight and only for type checking